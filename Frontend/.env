# REACT_APP_MY_BACKEND_DOMAIN_ENDPOINT=http://localhost:5000/api    ---- useful when serving with Node for local development, but for production, we will use the relative path to avoid CORS issues
REACT_APP_MY_BACKEND_DOMAIN_ENDPOINT=/api        # This will work for both development Nginx(with proxy) and production (with relative path)